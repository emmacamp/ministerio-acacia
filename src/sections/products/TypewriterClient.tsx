'use client';
import React from 'react';
import { Typewriter } from 'react-simple-typewriter';

export const TypewriterClient = () => {
  return <Typewriter words={['VENCEDORES']} cursor={false} loop />;
};
